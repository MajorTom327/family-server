# Configuration

In the explorer, only the needed folders are show. In most case, you don't need to update the hidden files. But you can need it.
If that the case, open the `.vscode/settings.json` file and toggle the required files
